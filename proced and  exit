#include <stdio.h>
void main()
{
 int x,a=0;
 do{
 printf("do you want to proceed\n");
 printf("enter 1 to proceed or 0 to exit");
 scanf("%d",&x);
 if(x==0) exit(0);
 
 printf("%d\n",a++);
 }while(x);
 
}
